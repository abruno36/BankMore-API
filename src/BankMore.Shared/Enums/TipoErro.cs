namespace BankMore.Shared.Enums
{
    public enum TipoErro
    {
        USER_UNAUTHORIZED,
        INVALID_DOCUMENT,
        INVALID_VALUE,
        CONTA_NOT_FOUND,
        SALDO_INSUFICIENTE,
        INTERNAL_ERROR,
        INACTIVE_ACCOUNT,
        INVALID_TYPE,
        INVALID_ACCOUNT
    }
}